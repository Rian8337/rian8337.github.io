/**
 * Determines how a spotlight should fill the screen. Only supported by `style4` and `style5` spotlight styles.
 */
export enum SpotlightScreenFill {
    /**
     * Fills the height of the screen.
     */
    fullscreen,

    /**
     * Fills half the height of the screen.
     */
    halfscreen,
}
