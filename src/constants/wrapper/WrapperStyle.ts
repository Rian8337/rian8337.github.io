/**
 * Available styles of a wrapper (undocumented by theme).
 */
export enum WrapperStyle {
    /**
     * A wrapper with padding and white background.
     */
    style1 = 1,

    /**
     * A wrapper with gray background.
     */
    style2,
}
