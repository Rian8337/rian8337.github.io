/**
 * Determines the orientation of the content inside a banner.
 */
export enum BannerOrientation {
    /**
     * Shows content on the left, image on the right.
     */
    left,

    /**
     * Shows content on the right, image on the left.
     */
    right,
}
