/**
 * Determines the position of the image inside a banner.
 */
export enum BannerImagePosition {
    /**
     * Left-positions image.
     */
    left,

    /**
     * Center-positions image.
     */
    center,

    /**
     * Right-positions image.
     */
    right,
}
