/**
 * Determines the size of items in an items element.
 */
export enum ItemsSize {
    /**
     * Uses small items.
     */
    small,

    /**
     * Uses medium items.
     */
    medium,

    /**
     * Uses big items.
     */
    big,
}
