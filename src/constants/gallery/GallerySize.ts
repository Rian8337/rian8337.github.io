/**
 * Determines the size of a thumbnail in a gallery section.
 */
export enum GallerySize {
    /**
     * Uses small thumbnails.
     */
    small,

    /**
     * Uses medium thumbnails.
     */
    medium,

    /**
     * Uses big thumbnails.
     */
    big,
}
